<Page x:Class="Beauty_Salon.Pages.PageServices"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Beauty_Salon.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageServices">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="56"/>
            <RowDefinition />
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="2"
Grid.ColumnSpan="3" HorizontalAlignment="Right">
            <TextBlock Text="Пользователь"/>
            <TextBlock x:Name="txtFullName"/>
        </StackPanel>
        <TextBox Name ="txtSearch" Grid.Row="1" Grid.Column="1"
VerticalAlignment="Center" Margin="5, 0, 5, 0" TextChanged="txtSearch_TextChanged"/>
        <ComboBox Name ="cmbSorting" Grid.Row="1" Grid.Column="2"
SelectedIndex="0" VerticalAlignment="Center" Margin="5, 0, 5, 0"
ItemsSource="{Binding SortingList}"
SelectionChanged="cmbSorting_SelectionChanged"/>
        <ComboBox Name ="cmbFilter" Grid.Row="1" Grid.Column="3"
SelectedIndex="0" VerticalAlignment="Center" Margin="5, 0, 5, 0"
ItemsSource="{Binding FilterList}"
SelectionChanged="cmbFilter_SelectionChanged"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="4"
VerticalAlignment="Center" HorizontalAlignment="Center">

        </StackPanel>

        <ListView Name="LViewProduct" Grid.Column="1" Grid.ColumnSpan="4"
Grid.Row="2" Margin="5,5,5,5" ScrollViewer.VerticalScrollBarVisibility="Auto"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=1}" SelectionChanged="LViewProduct_SelectionChanged" HorizontalAlignment="Left">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment"
Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1"
Background="{Binding Backgroud}" HorizontalAlignment="Left">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Image Margin="25,5,25,5" Source="{Binding MainImagePath}"/>
                            <StackPanel Grid.Column="1" Margin="5,5,5,5">
                                <TextBox Text="{Binding Title}" FontWeight="Bold"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="{Binding Cost}" TextDecorations="Strikethrough"/>
                                    <TextBox Text="{Binding CostWithDiscount, Mode=OneWay}" FontWeight="Bold"/>
                                    <TextBox Text="за" FontWeight="Bold"/>
                                    <TextBox Text="{Binding DurationInSeconds}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="* скидка "/>
                                    <TextBox Text="{Binding Discount}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>